namespace RPG.Core
{
    // Интерфейс ICollectable представляет объект, который можно собрать или подобрать.
    public interface ICollectable
    {
        // Метод Collect() предназначен для выполнения действий при сборе объекта.
        void Collect();
    }
}
